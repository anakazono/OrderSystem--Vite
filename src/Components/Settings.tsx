import "./settings.css"

export default function Settings({itemsArray, addItems, rmItems}){
  return(
    <div>
      <select>
        <option>1</option>
        <option>2</option>
      </select>
    </div>
  )
}
